server:
  name: "canvas-arkturian"
  type: "static-site"
  host: "arkturian.com"
  user: "root"
  port: 22
  deploy_path: "/var/www/canvas.arkturian.com"

backup:
  enabled: true
  dir: "/var/backups"
  prefix: "canvas-arkturian"

build:
  command: "npm run build"
  output_dir: "dist"
